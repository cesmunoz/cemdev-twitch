// eslint-disable-next-line import/prefer-default-export
export enum PARTITION_KEYS {
  COMMANDS = 'COMMANDS',
  HISTORIES = 'HISTORIES',
  COMMAND_TEMPLATE = 'COMMANDS_TEMPLATE',
  REQUESTS = 'REQUEST',
}
